# Template for project-secrets.yml (which is gitignored).
# Copy to project-secrets.yml and fill in your AdMob App ID.
targets:
  Waterday:
    info:
      properties:
        GADApplicationIdentifier: ca-app-pub-XXXXXXXXXXXXXXXX~XXXXXXXXXX
